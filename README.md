# WindowSight-Skill
WindowSight extracts structured UI info from Windows windows via UIA, outputting JSON: control tree, properties, focused element, and process info. Auto-detects window types (browser/Qt/WinRT/WebView2). Supports interactive-only filtering and one-shot screenshots. For UI automation testing, debugging, accessibility audits, and LLM-driven analysis.
